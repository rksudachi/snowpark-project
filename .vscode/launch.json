{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug my_proc",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      // "module": "{{workspaceFolder}}/src/my_proc",
      "console": "integratedTerminal",
      "env": {
          "PYTHONPATH": "${workspaceFolder}/src;${workspaceFolder}/deploy"
      },
    }  ]
}